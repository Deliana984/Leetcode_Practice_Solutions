# Write your MySQL query statement below
select Project.project_id, ROUND(AVG(Employee.experience_years),2) average_years from Project JOIN Employee ON Project.employee_id = Employee.employee_id group by Project.project_id;